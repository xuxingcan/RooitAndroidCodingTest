package com.cyning.rooitandroidcodingtest.ui

import androidx.appcompat.app.AppCompatActivity

/**
 * we dont have much more activities, so the base is empty
 */
abstract class BaseActivity : AppCompatActivity() {
}